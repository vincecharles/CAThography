<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CAThography - Philippine Transit Navigator</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="app-container">
        <header>
            <h1><i class="fas fa-map-marked-alt"></i> CAThography</h1>
            <p>Navigate Philippine public transportation with ease</p>
        </header>

        <main>
            <div class="search-container">
                <div class="location-input">
                    <i class="fas fa-location-arrow"></i>
                    <input type="text" id="current-location" placeholder="Current Location" readonly>
                    <button id="locate-me" title="Use my current location">
                        <i class="fas fa-crosshairs"></i>
                    </button>
                </div>

                <div class="location-input">
                    <i class="fas fa-map-marker-alt"></i>
                    <input type="text" id="destination-input" placeholder="Enter your destination">
                </div>

                <button id="search-route" class="primary-button">
                    <i class="fas fa-route"></i> Find Route
                </button>
            </div>

            <div class="map-container">
                <div id="map"></div>
                <div class="map-controls">
                    <button id="zoom-in" title="Zoom in">
                        <i class="fas fa-plus"></i>
                    </button>
                    <button id="zoom-out" title="Zoom out">
                        <i class="fas fa-minus"></i>
                    </button>
                </div>
            </div>

            <div class="route-info">
                <div class="info-card">
                    <h3><i class="fas fa-info-circle"></i> Route Information</h3>
                    <div class="info-grid">
                        <div class="info-item">
                            <span class="label">Distance:</span>
                            <span id="distance">-</span>
                        </div>
                        <div class="info-item">
                            <span class="label">Estimated Time:</span>
                            <span id="time">-</span>
                        </div>
                    </div>
                </div>

                <div class="transport-options">
                    <h3><i class="fas fa-bus"></i> Available Transportation</h3>
                    <div id="transport-list" class="transport-list">
                        <!-- Transport options will be dynamically added here -->
                    </div>
                </div>
            </div>
        </main>

        <footer>
            <p>Powered by OpenLayers and GTFS data</p>
            <p>&copy; 2025 CAThography. All rights reserved.</p>
        </footer>
    </div>

    <script type="module" src="app.js"></script>
</body>
</html> 